{
  "name": "bookshelf-mini-app",
  "version": "1.0.0",
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name=bookshelf"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
